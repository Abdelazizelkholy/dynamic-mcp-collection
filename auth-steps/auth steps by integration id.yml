info:
  name: "auth steps by integration id "
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}integrations/2/auth-steps"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
