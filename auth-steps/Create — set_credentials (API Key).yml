info:
  name: Create — set_credentials (API Key)
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}integrations/1/auth-steps"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Set Credentials",
        "step_type": "set_credentials",
        "auth_type": "api_key",
        "http_method": "POST",
        "base_endpoint_url": "https://api.example.com/auth",
        "inputs": [
          {
            "key": "api_key",
            "label": "API Key",
            "type": "password",
            "required": true,
            "placeholder": "sk_live_..."
          },
          {
            "key": "environment",
            "label": "Environment",
            "type": "select",
            "required": true,
            "options": [
              {
                "label": "Production",
                "value": "production"
              },
              {
                "label": "Sandbox",
                "value": "sandbox"
              }
            ]
          }
        ],
        "outputs": [
          "api_key",
          "environment"
        ],
        "response_example": {
          "api_key": "sk_live_...",
          "environment": "production"
        },
        "is_active": true
      }
  auth:
    type: bearer
    token: 13|L9anCETLDQ6RHfBZ7XPeBC6HmN0kfWS1znCeEAoY33ddb606

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
