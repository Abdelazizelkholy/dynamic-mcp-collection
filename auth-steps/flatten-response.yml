info:
  name: flatten-response
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}integrations/1/auth-steps/flatten-response"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
