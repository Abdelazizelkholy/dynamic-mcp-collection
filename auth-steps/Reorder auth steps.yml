info:
  name: Reorder auth steps
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}integrations/8/auth-steps/reorder"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |
      {
        "ordered_ids": [6, 5]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
