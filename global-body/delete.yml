info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}integrations/1/global-body/3"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "body": [
              {
                  "key": "store_id",
                  "require_from": "user_integration",
                  "value": "store_id",
                  "label": "Store ID",
                  "description": "Updated description"
              }
          ]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
