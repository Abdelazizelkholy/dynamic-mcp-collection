info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}integrations/1/global-body/1"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "key": "store_id",
        "require_from": "admin",
        "value": "store_id",
        "label": "Store ID",
        "description": "Updated description"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
