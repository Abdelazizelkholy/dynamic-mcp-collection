info:
  name: Get headers by integration id copy
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}integrations/1/global-body"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
