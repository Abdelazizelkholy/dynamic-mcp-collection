info:
  name: delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}users/4"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
