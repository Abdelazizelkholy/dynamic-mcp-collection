info:
  name: List service headers
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}integrations/1/services/1/headers"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
