info:
  name: Delete service header
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}integrations/1/services/1/headers/1"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
