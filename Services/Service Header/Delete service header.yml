info:
  name: Delete service header
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}integrations/1/services/2/headers/3"
  body:
    type: json
    data: |-
      {
          "label": "Updated Label",
          "is_active": false
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
