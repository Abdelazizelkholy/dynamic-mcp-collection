info:
  name: show service header
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}integrations/1/services/2/headers/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
