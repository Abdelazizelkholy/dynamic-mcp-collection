info:
  name: Create — Normal (static value by admin)
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}integrations/1/services/3/headers"
  body:
    type: json
    data: |-
      {
          "type": "normal",
          "header_key": "X-Api-Version",
          "require_from": "admin",
          "value": "2024-01",
          "label": "API Version",
          "description": "Fixed API version",
          "is_active": true
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
