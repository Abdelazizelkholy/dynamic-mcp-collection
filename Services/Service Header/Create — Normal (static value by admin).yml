info:
  name: Create — Normal (static value by admin)
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}integrations/1/services/1/headers"
  body:
    type: json
    data: |-
      {
        "headers": [
          {
            "type": "normal",
            "header_key": "X-Store-Id",
            "concatenate_key": null,
            "require_from": "user_integration",
            "value": "store_id",
            "label": "Store ID",
            "description": "Store identifier",
            "is_active": true
          }
        ]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
