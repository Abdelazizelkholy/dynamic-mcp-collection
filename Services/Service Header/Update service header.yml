info:
  name: Update service header
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}integrations/1/services/1/headers"
  body:
    type: json
    data: |-
      {
          "headers": [
              {
                  "type": "normal",
                  "header_key": "X-Api-Key",
                  "concatenate_key": null,
                  "require_from": "admin",
                  "value": "sk_live_abc123",
                  "label": "API Key",
                  "description": "Static API key",
                  "is_active": true
              }
          ]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
