info:
  name: Delete service
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}integrations/1/services/1"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
