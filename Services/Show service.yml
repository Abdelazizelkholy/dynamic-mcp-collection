info:
  name: Show service
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}integrations/1/services/1"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
