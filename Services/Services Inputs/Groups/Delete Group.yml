info:
  name: Delete Group
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}integrations/1/services/1/input-groups/1"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "key_name": "order_items",
          "data_type": "array_of_objects"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
