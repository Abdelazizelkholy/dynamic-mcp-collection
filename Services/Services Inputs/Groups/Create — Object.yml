info:
  name: Create — Object
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}integrations/1/services/1/input-groups"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "key_name": "address",
          "data_type": "object"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
