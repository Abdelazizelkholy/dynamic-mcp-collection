info:
  name: Show group
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}integrations/1/services/1/input-groups/1"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
