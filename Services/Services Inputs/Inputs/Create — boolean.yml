info:
  name: Create — boolean
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}integrations/1/services/1/inputs"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |
      {
          "field_type": "boolean",
          "key": "send_notification",
          "placeholder": null,
          "key_type": "body",
          "validation": "sometimes",
          "require_from": "user"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
