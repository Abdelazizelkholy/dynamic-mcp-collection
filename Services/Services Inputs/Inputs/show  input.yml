info:
  name: show  input
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}integrations/1/services/1/inputs/8"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
