info:
  name: List services params
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}integrations/1/services/1/params"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
