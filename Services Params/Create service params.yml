info:
  name: Create service params
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}integrations/1/services/2/params"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "type": "static",
          "value": "test"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
