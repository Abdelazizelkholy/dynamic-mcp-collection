info:
  name: Update service params
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}integrations/1/services/2/params/3"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: type
        type: text
        value: static
      - name: value
        type: text
        value: test
      - name: _method
        type: text
        value: PUT
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
