info:
  name: Check service (  chat )
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}user-integrations/services/2/status"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
