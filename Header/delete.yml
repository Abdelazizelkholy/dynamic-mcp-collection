info:
  name: delete
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}integrations/1/headers/54"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
