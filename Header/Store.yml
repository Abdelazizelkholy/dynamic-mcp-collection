info:
  name: Store
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}integrations/1/headers"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "headers": [
          {
                  "type": "basic_auth",
                  "header_key": "Authorization",
                  "require_from": "admin",
                  "value": "my_username",
                  "label": "username",
                  "description": "Basic auth username",
                  "is_active": true
              },
              {
                  "type": "basic_auth",
                  "header_key": "Authorization",
                  "require_from": "admin",
                  "value": "my_password",
                  "label": "password",
                  "description": "Basic auth password",
                  "is_active": true
              }
        ]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
